import SectionWrapper from '../ui/SectionWrapper';

export default function Hero() {
  return (
    <SectionWrapper>
      <div className="h-[80vh] rounded-3xl border border-[#2E2E2E] flex items-center justify-center">
        Hero Section
      </div>
    </SectionWrapper>
  );
}
